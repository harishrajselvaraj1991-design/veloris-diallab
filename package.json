{
  "name": "@veloris/diallab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "node scripts/build.mjs",
    "verify": "npm run typecheck && npm test && npm run build"
  },
  "engines": {
    "node": ">=22"
  }
}
